<IfModule mod_proxy.c>

    ProxyRequests On

    <Proxy *>
        Order deny,allow
        Allow from none
        Deny from all
        AuthType Basic
        AuthName "Just A Test"
        AuthUserFile /etc/apache2/htpasswd
        Require valid-user
        Satisfy Any
    </Proxy>

</IfModule>
